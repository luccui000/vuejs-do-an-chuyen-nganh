const state = {
    posts: [],
};
const mutations = {};
const actions = {};
const getters = {};

const post = {
    state: () => {
        return state;
    },
    mutations,
    actions,
    getters,
};
export default post;
